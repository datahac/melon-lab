copyright: Melonport AG
appId: com.melonproject.app
productName: Melon Manager Interface
artifactName: melon-manager-interface-${version}-${os}.${ext}
asar: false
extends: null

files:
  - package.json
  - from: build/main
    to: main
  - from: build/renderer
    to: renderer

directories:
  output: build/package
  buildResources: renderer/static

linux:
  category: Finance
  target:
    - deb
    - AppImage

mac:
  identity: null # TODO: Remove this for production
  category: public.app-category.finance
  target:
    - dmg
    - zip
